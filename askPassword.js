let password;

// do {
//     password = prompt("Enter the password:");
// } while (password !== "secret");

while (password !== "secret"){
    password = prompt("Enter the password:");
}
alert("Correct password!");

